[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "project-cognitive-layer"
version = "0.1.0"
description = "A local-first, model-agnostic project cognitive layer for long-term AI participation."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Project Cognitive Layer contributors"}]
dependencies = []

[project.scripts]
aiproj = "aiproj.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
